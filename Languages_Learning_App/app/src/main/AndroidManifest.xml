<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.languages_learning_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Languages_Learning_App">
        <activity android:name=".Controllers.ChangePasswordActivity"></activity>
        <activity android:name=".Controllers.AdminMainActivity" />
        <activity android:name=".Controllers.MainActivity" />
        <activity android:name=".Controllers.FogotPasswordActivity" />
        <activity android:name=".Controllers.ProfileActivity" />
        <activity android:name=".Controllers.RegisterUserActivity" />
        <activity android:name=".Controllers.AudioActivity" />
        <activity android:name=".Controllers.ChooseLanguageActivity" />
        <activity android:name=".Controllers.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>